// Reine Client-PWA (SPA): kein Server-Side-Rendering, kein Prerendering.
// Die Daten liegen lokal im Browser (IndexedDB/Dexie) — der Client rendert alles selbst.
export const ssr = false;
export const prerender = false;
